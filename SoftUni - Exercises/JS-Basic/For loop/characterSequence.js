function characterSequence(input) {
    
    let data = input[0];

    for (let i=0; i < data.length; i++ ) {
        let letter = data[i];
        console.log(letter);

    }
}
characterSequence(["ice cream"]);